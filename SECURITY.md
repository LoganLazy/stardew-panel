# 安全说明

## 支持范围

安全修复仅针对当前 `master` 分支和最新发布版本。

## 报告漏洞

请不要在公开 Issue 中披露尚未修复的漏洞。通过 GitHub 的私有漏洞报告功能联系维护者，并附上：

- 受影响版本和部署方式
- 复现步骤或最小复现代码
- 可能的影响
- 建议修复方式（如有）

## 部署安全边界

面板为了编排游戏容器会挂载宿主机的 Docker socket。这相当于授予容器宿主机级 Docker 权限，因此：

- 不要将面板端口直接暴露到公网
- 优先通过 Tailscale、WireGuard 或 SSH 隧道访问
- 使用公网反向代理时必须启用 HTTPS 和额外访问控制
- 必须设置强 `ADMIN_PASSWORD`、`VNC_PASSWORD` 和随机 `API_KEY`
- 不要提交 `docker/.env`、数据库、存档、Steam 会话或日志
